name: CI

on:
    # push:
    #     branches: ["main"]
    # pull_request:
    #     branches: ["main"]
    workflow_dispatch:

jobs:
    Docker:
        runs-on: ubuntu-latest
        steps:
            - name: Build
              run: echo "Hello World"
